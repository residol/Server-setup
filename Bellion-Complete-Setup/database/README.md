# Bellion Database

Questa directory contiene lo schema SQL e i dati di esempio per la network Bellion.

- `bellion_schema.sql`: Schema completo per MySQL

Importa questo file nel tuo server MySQL prima di avviare la network. Tutti i plugin e sistemi cross-server si basano su questo schema.